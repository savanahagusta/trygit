<%= form_with(model: major, local: true) do |form| %>
  <% if major.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(major.errors.count, "error") %> prohibited this major from being saved:</h2>

      <ul>
      <% major.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id_jur %>
    <%= form.number_field :id_jur %>
  </div>

  <div class="field">
    <%= form.label :jurusan %>
    <%= form.text_field :jurusan %>
  </div>

  <div class="field">
    <%= form.label :prodi %>
    <%= form.text_field :prodi %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
